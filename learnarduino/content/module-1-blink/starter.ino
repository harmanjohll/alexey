// Module 1: Blink
// Make the built-in LED on pin 13 blink at 1Hz
//
// Your task:
// 1. In setup(), set pin 13 as OUTPUT
// 2. In loop(), turn LED on, wait 500ms, turn off, wait 500ms

void setup() {
  // Configure pin 13 as an output

}

void loop() {
  // Turn the LED on (HIGH)

  // Wait 500 milliseconds

  // Turn the LED off (LOW)

  // Wait 500 milliseconds

}
